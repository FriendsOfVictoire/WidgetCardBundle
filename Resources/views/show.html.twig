{% extends 'VictoireCoreBundle:Widget:show.html.twig' %}

{% block content %}
    {% set themeCard = themeCard|default('picture') %}
    {% set template = "VictoireWidgetCardBundle:includes:#{themeCard}.html.twig" %}

    {% if link.url is not defined %}
        {% set link = link|merge(['url', vic_link_url(link.parameters)]) %}
    {% endif %}

    {% include template %}
{% endblock content %}
